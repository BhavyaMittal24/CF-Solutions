#include<bits/stdc++.h>
using namespace std;

int main(){
    int n, m, k;

    cin >> n >> m >> k;

    if( k == n || k == 1){
        int remainingBases = n-1;

        int secureBases = 1;

        int daysLeft = m;
        int maxSecureBases = 1;
        while(secureBases <= n &&  daysLeft){
            maxSecureBases = max(maxSecureBases, secureBases);

            if()
        }
    }
}